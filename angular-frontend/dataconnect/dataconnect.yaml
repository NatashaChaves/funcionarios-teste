specVersion: "v1"
serviceId: "angular-frontend"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-frontend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
